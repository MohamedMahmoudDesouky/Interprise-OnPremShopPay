{{- if .Values.orderService.enabled }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.orderService.name }}
  namespace: {{ .Values.namespaces.order }}
  labels:
    app.kubernetes.io/name: {{ .Values.orderService.name }}
    app.kubernetes.io/part-of: shoppay

spec:
  replicas: {{ .Values.orderService.replicaCount }}

  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.orderService.name }}

  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.orderService.name }}
        app.kubernetes.io/part-of: shoppay

    spec:
      serviceAccountName: {{ .Values.serviceAccounts.orderService.name }}
      automountServiceAccountToken: false

      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: {{ .Values.orderService.name }}
          image: "{{ .Values.orderService.image.repository }}:{{ .Values.orderService.image.tag }}"
          imagePullPolicy: {{ .Values.orderService.image.pullPolicy }}

          ports:
            - name: http
              containerPort: {{ .Values.orderService.service.targetPort }}
              protocol: TCP

          env:
            - name: PORT
              value: {{ .Values.orderService.env.port | quote }}

            - name: PAYMENT_SERVICE_URL
              value: {{ .Values.orderService.env.paymentServiceUrl | quote }}

            - name: ORDER_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.orderService.env.databaseSecretName }}
                  key: {{ .Values.orderService.env.databasePasswordKey }}

            - name: DATABASE_URL
              value: "postgresql://{{ .Values.orderService.env.databaseUser }}:$(ORDER_DB_PASSWORD)@{{ .Values.orderService.env.databaseHost }}:{{ .Values.orderService.env.databasePort }}/{{ .Values.orderService.env.databaseName }}"

          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3

          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 12

          resources:
{{ toYaml .Values.orderService.resources | indent 12 }}

          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.orderService.name }}
  namespace: {{ .Values.namespaces.order }}
  labels:
    app.kubernetes.io/name: {{ .Values.orderService.name }}
    app.kubernetes.io/part-of: shoppay
  annotations:
    ingress.kubernetes.io/service-upstream: "true"

spec:
  type: {{ .Values.orderService.service.type }}

  selector:
    app.kubernetes.io/name: {{ .Values.orderService.name }}

  ports:
    - name: http
      port: {{ .Values.orderService.service.port }}
      targetPort: http
      protocol: TCP

{{- end }}
