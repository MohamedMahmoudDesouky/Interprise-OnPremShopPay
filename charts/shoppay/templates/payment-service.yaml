{{- if .Values.paymentService.enabled }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.paymentService.name }}
  namespace: {{ .Values.namespaces.payment }}
  labels:
    app.kubernetes.io/name: {{ .Values.paymentService.name }}
    app.kubernetes.io/part-of: shoppay

spec:
  replicas: {{ .Values.paymentService.replicaCount }}

  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.paymentService.name }}

  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.paymentService.name }}
        app.kubernetes.io/part-of: shoppay

    spec:
      serviceAccountName: {{ .Values.serviceAccounts.paymentService.name }}
      automountServiceAccountToken: false

      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: {{ .Values.paymentService.name }}
          image: "{{ .Values.paymentService.image.repository }}:{{ .Values.paymentService.image.tag }}"
          imagePullPolicy: {{ .Values.paymentService.image.pullPolicy }}

          ports:
            - name: http
              containerPort: {{ .Values.paymentService.service.targetPort }}
              protocol: TCP

          env:
            - name: PORT
              value: {{ .Values.paymentService.env.port | quote }}

            - name: PAYMENT_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.paymentService.env.databaseSecretName }}
                  key: {{ .Values.paymentService.env.databasePasswordKey }}

            - name: DATABASE_URL
              value: "postgresql://{{ .Values.paymentService.env.databaseUser }}:$(PAYMENT_DB_PASSWORD)@{{ .Values.paymentService.env.databaseHost }}:{{ .Values.paymentService.env.databasePort }}/{{ .Values.paymentService.env.databaseName }}"

          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3

          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 12

          resources:
{{ toYaml .Values.paymentService.resources | indent 12 }}

          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.paymentService.name }}
  namespace: {{ .Values.namespaces.payment }}
  labels:
    app.kubernetes.io/name: {{ .Values.paymentService.name }}
    app.kubernetes.io/part-of: shoppay

spec:
  type: {{ .Values.paymentService.service.type }}

  selector:
    app.kubernetes.io/name: {{ .Values.paymentService.name }}

  ports:
    - name: http
      port: {{ .Values.paymentService.service.port }}
      targetPort: http
      protocol: TCP

{{- end }}
