stages:
  - validate
  - build
  - security

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: ""
  IMAGE_TAG: $CI_COMMIT_SHORT_SHA
  DOCKER_BUILDKIT: "1"
  DOCKER_IMAGE_DIR: "build-artifacts"

default:
  interruptible: true

workflow:
  rules:
    - if: $CI_COMMIT_BRANCH == "dev"
    - if: $CI_COMMIT_BRANCH == "main"

# =========================
# SMART CACHING (IMPROVED)
# =========================

cache:
  key: "$CI_PROJECT_NAME-$CI_COMMIT_REF_SLUG"
  paths:
    - frontend/node_modules/
    - backend/services/product-service/node_modules/
    - backend/services/order-service/node_modules/
    - backend/services/payment-service/node_modules/
    - backend/services/contact-service/node_modules/

# =========================
# VALIDATION STAGE
# =========================

frontend_lint:
  stage: validate
  image: node:22-alpine
  rules:
    - changes:
        - frontend/**/*
  script:
    - cd frontend
    - npm ci
    - npm run lint

frontend_build:
  stage: validate
  image: node:22-alpine
  rules:
    - changes:
        - frontend/**/*
  script:
    - cd frontend
    - npm ci
    - npm run build

# =========================
# BACKEND VALIDATION
# =========================

.backend_template: &backend_template
  image: node:22-alpine
  stage: validate

backend_product_build:
  <<: *backend_template
  rules:
    - changes:
        - backend/services/product-service/**/*
  script:
    - cd backend/services/product-service
    - npm ci --omit=dev
    - node --check src/index.js

backend_order_build:
  <<: *backend_template
  rules:
    - changes:
        - backend/services/order-service/**/*
  script:
    - cd backend/services/order-service
    - npm ci --omit=dev
    - node --check src/index.js

backend_payment_build:
  <<: *backend_template
  rules:
    - changes:
        - backend/services/payment-service/**/*
  script:
    - cd backend/services/payment-service
    - npm ci --omit=dev
    - node --check src/index.js

backend_contact_build:
  <<: *backend_template
  rules:
    - changes:
        - backend/services/contact-service/**/*
  script:
    - cd backend/services/contact-service
    - npm ci --omit=dev
    - node --check src/index.js

# =========================
# HELM VALIDATION
# =========================

helm_validate:
  stage: validate
  image: alpine:3.20
  rules:
    - changes:
        - charts/**/*
  before_script:
    - apk add --no-cache curl bash git openssl
    - curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
  script:
    - helm lint charts/shoppay
    - helm template shoppay charts/shoppay > rendered.yaml
  artifacts:
    paths:
      - rendered.yaml

# =========================
# DOCKERFILE LINT
# =========================

dockerfile_lint:
  stage: validate
  image: hadolint/hadolint:latest-debian
  rules:
    - changes:
        - "**/Dockerfile"
  script:
    - hadolint frontend/Dockerfile
    - hadolint backend/services/product-service/Dockerfile
    - hadolint backend/services/order-service/Dockerfile
    - hadolint backend/services/payment-service/Dockerfile
    - hadolint backend/services/contact-service/Dockerfile

# =========================
# BUILD STAGE (NOW WITH ARTIFACTS)
# =========================

.build_template:
  stage: build
  image: docker:27.0.3
  services:
    - docker:27.0.3-dind
  before_script:
    - docker info
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
    - mkdir -p $DOCKER_IMAGE_DIR

docker_build_product:
  extends: .build_template
  rules:
    - changes:
        - backend/services/product-service/**/*
  script:
    - docker build -t $DOCKERHUB_USERNAME/shoppay-product:$IMAGE_TAG ./backend/services/product-service
    - docker push $DOCKERHUB_USERNAME/shoppay-product:$IMAGE_TAG
    - echo "$DOCKERHUB_USERNAME/shoppay-product:$IMAGE_TAG" > $DOCKER_IMAGE_DIR/product.txt
  artifacts:
    paths:
      - build-artifacts/product.txt
    expire_in: 1 hour

docker_build_order:
  extends: .build_template
  rules:
    - changes:
        - backend/services/order-service/**/*
  script:
    - docker build -t $DOCKERHUB_USERNAME/shoppay-order:$IMAGE_TAG ./backend/services/order-service
    - docker push $DOCKERHUB_USERNAME/shoppay-order:$IMAGE_TAG
    - echo "$DOCKERHUB_USERNAME/shoppay-order:$IMAGE_TAG" > $DOCKER_IMAGE_DIR/order.txt
  artifacts:
    paths:
      - build-artifacts/order.txt
    expire_in: 1 hour

docker_build_payment:
  extends: .build_template
  rules:
    - changes:
        - backend/services/payment-service/**/*
  script:
    - docker build -t $DOCKERHUB_USERNAME/shoppay-payment:$IMAGE_TAG ./backend/services/payment-service
    - docker push $DOCKERHUB_USERNAME/shoppay-payment:$IMAGE_TAG
    - echo "$DOCKERHUB_USERNAME/shoppay-payment:$IMAGE_TAG" > $DOCKER_IMAGE_DIR/payment.txt
  artifacts:
    paths:
      - build-artifacts/payment.txt
    expire_in: 1 hour

docker_build_contact:
  extends: .build_template
  rules:
    - changes:
        - backend/services/contact-service/**/*
  script:
    - docker build -t $DOCKERHUB_USERNAME/shoppay-contact:$IMAGE_TAG ./backend/services/contact-service
    - docker push $DOCKERHUB_USERNAME/shoppay-contact:$IMAGE_TAG
    - echo "$DOCKERHUB_USERNAME/shoppay-contact:$IMAGE_TAG" > $DOCKER_IMAGE_DIR/contact.txt
  artifacts:
    paths:
      - build-artifacts/contact.txt
    expire_in: 1 hour

docker_build_frontend:
  extends: .build_template
  rules:
    - changes:
        - frontend/**/*
  script:
    - docker build --build-arg NEXT_PUBLIC_API_BASE_URL=/api \
      -t $DOCKERHUB_USERNAME/shoppay-frontend:$IMAGE_TAG ./frontend
    - docker push $DOCKERHUB_USERNAME/shoppay-frontend:$IMAGE_TAG
    - echo "$DOCKERHUB_USERNAME/shoppay-frontend:$IMAGE_TAG" > $DOCKER_IMAGE_DIR/frontend.txt
  artifacts:
    paths:
      - build-artifacts/frontend.txt
    expire_in: 1 hour

# =========================
# SECURITY STAGE (NOW READS ARTIFACTS)
# =========================

.trivy_template:
  stage: security
  image:
    name: aquasec/trivy:latest
    entrypoint: [""]

# Secrets scan (global)
trivy_secrets:
  extends: .trivy_template
  script:
    - trivy fs --scanners secret --exit-code 1 .

# K8s scan selective
trivy_k8s_config:
  extends: .trivy_template
  rules:
    - changes:
        - charts/**/*
  script:
    - trivy config --severity HIGH,CRITICAL --exit-code 1 charts/shoppay

# =========================
# IMAGE SCANS (ARTIFACT-BASED)
# =========================

trivy_product_image:
  extends: .trivy_template
  dependencies:
    - docker_build_product
  script:
    - IMAGE=$(cat build-artifacts/product.txt)
    - trivy image --severity HIGH,CRITICAL --exit-code 1 $IMAGE

trivy_order_image:
  extends: .trivy_template
  dependencies:
    - docker_build_order
  script:
    - IMAGE=$(cat build-artifacts/order.txt)
    - trivy image --severity HIGH,CRITICAL --exit-code 1 $IMAGE

trivy_payment_image:
  extends: .trivy_template
  dependencies:
    - docker_build_payment
  script:
    - IMAGE=$(cat build-artifacts/payment.txt)
    - trivy image --severity HIGH,CRITICAL --exit-code 1 $IMAGE

trivy_contact_image:
  extends: .trivy_template
  dependencies:
    - docker_build_contact
  script:
    - IMAGE=$(cat build-artifacts/contact.txt)
    - trivy image --severity HIGH,CRITICAL --exit-code 1 $IMAGE

trivy_frontend_image:
  extends: .trivy_template
  dependencies:
    - docker_build_frontend
  script:
    - IMAGE=$(cat build-artifacts/frontend.txt)
    - trivy image --severity HIGH,CRITICAL --exit-code 1 $IMAGE